# 프로젝트 인계 프롬프트: 필드워 (가칭)

아래는 지금까지 Claude(claude.ai)와 함께 설계/구현한 실시간 멀티플레이 카드게임 프로젝트입니다.
이 문서를 읽고 **현재 상태를 파악한 뒤, "다음 단계"부터 이어서 개발**해주세요.

---

## 1. 프로젝트 개요

- **게임명(가칭)**: 필드워
- **컨셉**: 오행(五行) 속성 기반 실시간 마법사 대전 카드게임
- **인원**: 2~6명, 실시간 동기화 (같은 방에서 다 같이 진행)
- **로그인**: 없음. 닉네임만 입력하고 바로 방 생성/입장
- **DB**: MVP 단계에서는 사용하지 않음. 모든 게임 상태는 서버 메모리(Node.js 프로세스 내 `Map`)에서 관리
- **스택**: Node.js + Express + Socket.io (백엔드), Vanilla JS/HTML/CSS (프론트, 프레임워크 없음)
- **개발자 배경 참고**: 이 프로젝트를 진행하는 사람은 Java/Spring Boot에 익숙하고 Node.js는 이번이 첫 시도에 가깝습니다. 설명이 필요할 때는 Spring Boot 개념과 비교해주면 이해가 빠릅니다.

---

## 2. 게임 룰 (확정된 내용)

### 기본 구조
- HP만 사용 (MP, 골드 등 다른 스탯은 없음, 확장 여지만 열어둠)
- 카드는 랜덤 지급, 손패 상한 존재 (구체적 수치는 아직 미정)
- 승리 조건: 최후의 1인(또는 팀) 생존

### 턴 진행
- 순서대로 턴이 돌아감 (고정된 순번)
- 자기 턴에는 **아무 상대나 지정해서 공격 가능** (정해진 상대 하나만 때리는 방식 아님)
- 공격을 받으면 **즉시 방어턴이 삽입**됨 → 방어자가 방어 카드를 내거나(또는 포기) → 결과 확정 → 다음 사람 턴으로 순서 복귀

### 코스트 시스템 (2026-08-25, 전투 시스템 개편으로 갱신 — 아래 "코스트 매칭 방어" 방식은 폐기됨)
- **코스트는 "행동 자원 풀"입니다.** 카드를 낼 수 있는지(자원이 충분한지)만 결정하고, 방어 성공 여부/데미지 계산에는 관여하지 않음
- **공격 코스트 풀**과 **방어 코스트 풀**은 서로 다른 자원이며 독립적으로 관리됨. 기본 지급량은 각각 **3** (개발자와 상의해서 확정)
  - 공격 코스트: 자기 턴에 지급, 그 풀 안에서 여러 번·여러 대상에게 공격 카드를 낼 수 있음 (예: 1코스트 카드 3번 / 3코스트 카드 1번 등 자유)
  - 방어 코스트: 방어자도 별도의 풀을 보유. 한 턴 동안 여러 번 공격받으면 그때마다 자신의 방어 코스트 풀에서 소모하며 대응 (집중 공격을 받으면 방어 코스트가 바닥날 수 있음)
  - 카드/필드 효과로 두 풀을 각각 증감시킬 수 있음 (나중에 확장 여지)
- **안 쓴 코스트는 다음 턴으로 이월되지 않고 매턴 리셋됨** (공격/방어 풀 둘 다)
- (검토했다가 보류한 것: "방어 성공 시 코스트가 적립되어 다음 턴 코스트가 늘어난다"는 메커니즘은 일단 제외했음. 나중에 확장으로 재검토 가능)

### 카드 스탯: 공격력 / 방어력
- 공격 카드는 **공격력**만, 방어 카드는 **방어력**만 가짐 (역할 분리). 공격력/방어력을 동시에 가지는 특수 카드는 나중에 추가될 수 있음 (지금은 데이터 구조상 두 필드를 다 가질 수 있게만 열어둠)

### 데미지 계산 방식
- 코스트는 "낼 수 있는가"만 결정, **실제 피해량은 공격력과 방어력의 수치 비교**로 계산
  - 공격력 10 vs 방어력 15 → 방어 성공, 데미지 0
  - 공격력 10 vs 방어력 5 → 데미지 5 (공격력 - 방어력, 음수면 0으로 clamp)
  - 방어 카드를 못 내거나(방어 코스트 부족) 포기하면 → 공격력 그대로 전부 피해

### 속성 시스템 (오행)
- 5속성: **목(나무) · 화(불) · 토(흙) · 금(쇠) · 수(물)**
- 광/암 속성은 도입하지 않기로 함 (제외)
- **상극(相剋) — 방어력 보너스에 사용** (2026-08-25 변경: 기존 "코스트 할인" 방식은 폐기)
  ```
  목 → 토 → 수 → 화 → 금 → (다시 목으로 순환)
  ```
  읽는 법: 화살표 앞의 속성이 화살표 뒤의 속성을 이긴다(카운터한다). 즉 목극토, 토극수, 수극화, 화극금, 금극목.
  방어 시: 방어 속성이 공격 속성을 상극하면 → **방어력에 고정 보너스 +5**를 더함 (개발자와 상의해서 확정한 수치). 상극이 아니면 보너스 없이 카드의 방어력 그대로 사용.

- **상생(相生) — 필드 지형과 연동하여 속성 강화에 사용**
  ```
  목 → 화 → 토 → 금 → 수 → (다시 목으로 순환)
  ```
  읽는 법: 목생화, 화생토, 토생금, 금생수, 수생목.
  특정 속성의 지형이 필드에 깔리면, 그 지형이 상생하는 속성의 마법이 강화됨.
  예: 목(나무) 지형이 활성화되어 있으면 → 목생화 → **화속성 마법이 강화됨**
  강화 방식: 상극 보너스와 같은 결로 **공격력/방어력에 수치 보너스**를 더함 (2026-08-25 확정).
  지금은 테스트 단계라 지형 카드마다 동일하게 **+5 고정값**을 쓰지만, 지형 카드 데이터에 `synergyBonus` 필드로 값을 따로 들고 있어서 **나중에 카드별로 다른 보너스 수치를 줄 수 있도록 구조를 열어둠** (개발자 요청사항)

### 필드 시스템
게임의 차별점. "필드"는 기본적으로 무속성 상태이며, 카드를 사용해 추가 효과를 부여하는 공유 환경.

카드 사용자를 포함한 **전원에게 공평하게 적용**되는 것이 원칙. 단, "필드 무시" 카드로 개별 회피 가능(아직 미구현).

| 구분 | 지형 카드 | 효과 카드 |
|---|---|---|
| 슬롯 | 1개 (교체형) | 여러 개 (누적형) |
| 지속 | 다른 지형 카드가 나오기 전까지 영구 | 카드에 명시된 턴 수 동안만 (시한부) |
| 중첩 | 불가 (항상 1개만 존재) | 가능 (여러 효과 동시 적용) |
| 적용 대상 | 전원 공평 (설치자 포함) | 전원 공평 (설치자 포함) |
| 예시 | 예: "용암지대" → 다른 지형 나오기 전까지 유지, 오행 상생으로 특정 속성 강화 | 예: "전류" (3턴 지속) → 3턴 동안 매턴 전원 소량 피해, 이후 자동 소멸. 다른 효과 카드와 중첩 가능 |

### 카드 3종 분류
1. **개인 카드** — 공격/방어/회복 마법 등, 특정 대상에게 사용
2. **필드 카드** — 지형(1슬롯 교체형) / 효과(다슬롯 중첩형)
3. **잡화** — 카드 교환, 필드 제거/강탈 등 (아직 구체적 카드 미설계)

### 아직 정해지지 않은 것 (다음 설계 시 논의 필요)
- 시작 HP, 손패 상한, 덱 크기 등 수치 밸런스
- 실제 카드 목록 (속성별 공격/방어/회복/필드 카드 구체적 스펙 — 지금은 최소 세트인 8장뿐)
- 상생 보너스의 카드별 최종 수치 (지금은 전부 +5로 동일, 밸런싱 단계에서 카드마다 다르게 조정 예정)
- 잡화 카드 종류
- "필드 무시" 카드의 구체적 동작

---

## 3. 기술 아키텍처

### 폴더 구조 (현재 실제 구현된 상태)
```
fieldwar/
├── package.json
├── server/
│   ├── index.js               # 서버 진입점 (Express + Socket.io 초기화)
│   ├── data/
│   │   └── cards.json         # 카드 마스터 데이터 (공격 3 + 방어 3 + 지형 1 + 효과 1 = 8장)
│   ├── socket/
│   │   ├── roomHandlers.js    # 방 생성/입장/퇴장 이벤트 처리 (구현 완료)
│   │   └── gameHandlers.js    # 턴 진행 + 카드 사용/방어/필드 이벤트 처리 (구현 완료)
│   └── game/
│       ├── Room.js            # 방 상태 클래스 (플레이어, 턴 순서, 손패, 덱, 코스트, 방어 대기, HP, 필드 관리)
│       └── elementTable.js    # 오행 상극/상생 테이블 (상극은 4단계, 상생은 5단계부터 실제 사용됨)
└── client/
    ├── index.html              # 로비/대기실/게임/게임종료 화면 + 카드 연출용 오버레이(#castOverlay, #combatOverlay) 마크업
    ├── main.js                 # Socket.io 클라이언트, 화면 전환 + 손패(부채꼴)/전장/전투 연출/전투 로그 렌더링
    └── style.css                # 반응형 스케일링(.game-board font-size clamp 기준 em 단위) + 카드/연출 스타일
```

### 설계 원칙 (반드시 지켜야 함)
- **서버가 모든 판정의 권위자(authoritative)**: 코스트 계산, 속성 상성 판정, HP 계산 등 게임 로직은 전부 서버에서 처리. 클라이언트는 상태를 받아 화면에 그리기만 함 (클라이언트 조작으로 부정 방지)
- **개인 정보는 개별 emit**: 상대방 손패 내용은 안 보이게, 장수만 공개하는 방식으로 설계해야 함 (아직 손패 개념 자체가 구현 전이라 이 부분은 카드 도입 시 신경써야 함)
- DB 없이 인메모리(`Map`)로 방 상태 관리. `rooms.js`의 `rooms` Map이 유일한 저장소.

### 현재까지 구현 완료된 기능
1. **방 생성/입장/퇴장**
   - `room:create`, `room:join`, `room:leave`, `disconnect` 이벤트 처리
   - 4자리 랜덤 방 코드 발급
   - 방장이 나가면 다음 사람에게 방장 자동 위임
   - 방에 아무도 없으면 메모리에서 방 삭제
2. **턴 진행**
   - `game:start`: 방장만 시작 가능, 2명 이상 필요, 입장 순서대로 `turnOrder` 고정, 상태를 `waiting → playing`으로 전환
   - `game:endTurn`: 현재 턴인 사람만 가능, 방어 대기(`pendingDefense`) 중이면 종료 불가, 다음 살아있는 사람에게 턴 넘김 (`Room.advanceTurn()`)
   - 클라이언트: 현재 턴인 사람에게 "지금은 내 턴입니다" 표시, 본인 턴일 때만 "턴 종료" 버튼 활성화
3. **카드 사용 → 방어 → HP 감소 흐름 (3단계, 2026-08-25 완료)**
   - `server/data/cards.json`: 공격 카드 3장(가시덩굴/화염구/해일), 방어 카드 3장(쇠사슬 방패/돌벽/물의 장막)
   - `game:start` 시 카드 풀을 종류당 6장씩 복사한 덱을 셔플해서 각 플레이어에게 5장씩(`HAND_SIZE`) 랜덤 분배. 손패는 `game:handUpdated`로 본인에게만 개별 전송 (상대에게는 `handCount`만 공개)
   - `game:playCard`: 자기 턴 + 방어 대기 중 아닐 때만 가능. 공격 카드를 손패에서 제거하고 `pendingDefense` 상태로 전환, 대상에게만 `game:defenseRequest` 전송
   - `game:defend` / `game:skipDefense`: 방어자만 가능. 포기하면 공격 카드의 공격력을 그대로 적용, 방어 성공 시 피해 0 (판정 규칙은 4단계에서 교체됨, 아래 참고) — **이 두 이벤트는 6단계에서 `game:confirmDefense`로 완전히 대체되어 현재는 존재하지 않음** (아래 "6단계 진행 상황" 및 이벤트 목록 참고)
   - `Room.applyDamage()`로 HP 차감 및 사망 처리(HP 0 → `isAlive: false`), `Room.getAlivePlayers()`로 생존자 1명 남으면 `game:over` 발동, `room.status`를 `ended`로 전환
   - 클라이언트: 전장(참가자 HP바 + 카드 장수) 표시, 손패에서 공격 카드 클릭 → 대상(다른 플레이어 카드) 클릭으로 공격 실행, 방어 요청 팝업(방어 카드 선택 or 포기), 전투 로그, 게임 종료 화면
   - 개발자가 브라우저에서 직접 플레이 테스트 완료 (2026-08-25)
4. **코스트 시스템 + 오행 상성 판정 (4단계, 2026-08-25 완료 — 같은 날 전투 시스템 개편으로 판정 로직이 다시 교체됨, 아래 4-b 참고)**
   - `Room.js`: 플레이어마다 코스트 필드 추가, `resetAllCosts()`로 전원 코스트를 리셋, `spendXxxCost(socketId, amount)`로 소모(부족하면 `false` 반환하고 아무 것도 안 함)
   - **설계 결정(중요, 지금도 유효함)**: 코스트 리셋은 "지금 턴인 사람만"이 아니라 **턴이 바뀔 때마다(`startGame`/`advanceTurn`) 전원**에게 적용됩니다. 이유: 방어는 자기 턴이 아닐 때도 발생하는데, 방어자도 방어 코스트를 지불할 수 있어야 하므로, 방어자의 코스트도 항상 최신 상태로 채워져 있어야 합니다. 다르게 가고 싶다면 `Room.resetAllCosts()` 호출부만 고치면 됩니다
   - `server/game/elementTable.js`: 상극(`COUNTER_MAP`)/상생(`GENERATE_MAP`) 테이블과 `isCounter(defenderElement, attackerElement)` 헬퍼. 이 시점(4단계)에는 상생이 아직 어디서도 안 쓰였으나, 5단계에서 필드 지형 보너스에 사용됨 (아래 5번 항목 참고)
   - (최초 구현했던 "공격 카드와 코스트가 같아야만 방어 가능, 상극이면 코스트 차액 지불로 할인" 방식은 **완전히 폐기**됨 — 아래 4-b 참고)

   ### 4-b. 전투 시스템 개편 (2026-08-25, `combat_mechanic_update_prompt.md` 지시로 진행)
   개발자가 claude.ai에서 별도로 설계한 내용을 `combat_mechanic_update_prompt.md`로 전달받아 반영함. **코스트 매칭 방어 방식은 폐기**, 아래 방식으로 교체:
   - **코스트 = 행동 자원 풀**로 의미가 바뀜 (매칭 판정에는 더 이상 안 쓰임). `Room.js`의 플레이어 필드를 `cost`/`maxCost` 단일 값에서 `attackCost`/`maxAttackCost`, `defenseCost`/`maxDefenseCost` **두 개의 독립된 풀**로 분리. 기본 지급량은 상의 후 **공격 3 / 방어 3**으로 확정
   - `cards.json`의 카드 스탯을 `damage` 단일 필드에서 `attackPower`/`defensePower`로 분리 (공격 카드는 `attackPower`만 값이 있고 `defensePower: 0`, 방어 카드는 반대)
   - **데미지 계산**: `damageDealt = max(0, 공격력 - (방어력 + 상극보너스))`. 상극(카운터) 관계면 방어력에 고정 보너스 `COUNTER_BONUS = 5`를 더함 (개발자와 상의해서 확정한 수치, `gameHandlers.js`에 상수로 정의됨)
   - `game:playCard`: 공격 카드 코스트만큼 **공격 코스트 풀**에서 소모 (부족하면 `INSUFFICIENT_COST`). 한 턴 안에서 공격 코스트가 남아있는 한 여러 번, 여러 대상에게 공격 가능 (이 부분은 원래도 막혀있지 않았어서 코드 변경 없이 이미 지원되던 흐름)
   - `game:defend`(현재는 `game:confirmDefense`로 대체됨, 아래 6단계 참고): 방어 카드 코스트만큼 **방어 코스트 풀**에서 소모 (부족하면 `INSUFFICIENT_COST`, 카드 소모 안 됨). 이제 "이 카드로는 아예 방어 불가"라는 경우가 없음 — 방어 코스트만 있으면 어떤 방어 카드든 낼 수 있고, 상성이 안 맞으면 그냥 보너스 없이 방어력만 적용됨 (`CARD_COST_MISMATCH` 에러 코드는 삭제됨)
   - `game:combatResult`에 `attackPower`, `defensePowerUsed`, `counterBonus` 필드 추가 — 클라이언트가 "완전 방어 / 부분 피해 / 전부 피해"를 구분해서 로그에 표시함
   - 클라이언트: 전장에 공격 코스트와 방어 코스트를 따로 표시 (`공격 2/3 · 방어 3/3`), 손패의 공격 카드는 공격 코스트 기준으로 회색 처리, 방어 요청 팝업의 방어 카드는 방어 코스트 기준으로 회색 처리 (단, 상성/데미지 계산 자체는 여전히 서버 전담 — 클라이언트는 "낼 수 있는 카드인지"만 미리 보여줌)
   - `Room`/`elementTable`을 직접 참조하는 인메모리 테스트 스크립트로 19개 시나리오(상극 완전방어, 비상극 부분피해, 방어 포기 시 전액피해, 공격/방어 코스트 부족 각각 거부, 한 턴 내 다중 공격, 턴 전환 시 두 풀 모두 리셋 등) 전부 통과 확인 후 삭제. 개발자가 브라우저에서 재테스트 완료

5. **필드(지형/효과) 시스템 (5단계, 2026-08-25 완료)**
   - `Room.js`: `this.terrain`(null 또는 `{ cardId, cardName, element, synergyBonus }`, 1슬롯 교체형) + `this.effects`(배열, 다슬롯 누적형) 추가
   - `cards.json`에 카드 타입 2종 신규 추가: `terrain`(지형, 예: 용암지대 — 화속성, `synergyBonus: 5`), `effect`(효과, 예: 전류 — 코스트1, 3턴 동안 매턴 전원에게 5의 피해를 주는 `tickDamage`/`durationTurns` 필드 보유)
   - **설계 결정**: 지형 카드는 코스트를 소모하지 않음(항상 `cost: 0`). 효과 카드는 코스트를 소모하는데, **공격 코스트 풀**에서 소모하기로 함 — 자기 턴에 능동적으로 내는 카드라 공격 카드와 같은 풀을 쓰는 게 자연스럽다고 판단 (개발자와 상의해서 결정)
   - `game:playFieldCard` `{ roomId, cardInstanceId }` 신규 이벤트: 대상 지정이 필요 없다는 점이 `game:playCard`와 다름. 지형이면 `Room.setTerrain()`으로 교체(기존 지형은 그냥 사라짐), 효과면 `Room.addEffect()`로 누적 추가
   - **상생 보너스 적용**: `Room.getTerrainSynergyBonus(cardElement)` — 깔린 지형이 상생하는 속성(`elementTable.js`의 `GENERATE_MAP` 이용)과 카드 속성이 일치하면 보너스 반환. `game:playCard`(공격력에 반영) / `game:defend`(방어력에 반영) 양쪽에서 호출하며, 상극 보너스와 독립적으로 합산됨(둘 다 해당되면 둘 다 더해짐)
   - **효과 발동**: `Room.tickEffects()` — `game:endTurn`으로 턴이 넘어갈 때마다 호출됨. 모든 효과가 생존자 전원에게 `tickDamage`만큼 피해를 주고 `remainingTurns`를 하나씩 줄이며, 0이 되면 자동 제거. 효과 피해로 게임이 끝날 수도 있어서 `maybeEndGame()` 헬퍼를 만들어 `resolveCombat`과 `game:endTurn` 양쪽에서 공용으로 씀 (기존에 `resolveCombat` 안에 있던 승리 판정 로직을 그대로 추출한 것)
   - `game:combatResult`에 `attackTerrainBonus`/`defenseTerrainBonus` 필드 추가로 로그에 "상극 +5, 지형 +5 포함" 식으로 표시됨
   - 클라이언트: 게임 화면에 필드 상태 표시줄(`🗺️ 용암지대 (화속성) · ⚡ 전류 (2턴 남음)`) 추가. 손패의 지형/효과 카드는 클릭하는 즉시 사용됨(대상 선택 없음), 효과 카드는 공격 코스트 기준으로 회색 처리
   - `Room`을 직접 참조하는 인메모리 테스트 스크립트로 17개 시나리오(지형 무코스트 설치, 지형 교체, 상생 보너스가 방어력에 반영, 효과 카드 코스트 소모/부족, 효과 발동 시 전원 피해 + 잔여 턴 감소 + 3턴 후 자동 소멸) 전부 통과 확인 후 삭제

6. **카드 자동 보충 + 필드 효과 가시성 + 보너스 강조색 (5단계 완료 직후 개발자 피드백으로 추가, 2026-08-25)**
   - **카드 자동 보충**: `Room.js`에 `this.deck`(cardId 배열) 추가. `dealHandsToRoom()`이 게임 시작 시 손패를 나눠주고 남은 카드를 `room.setDeck()`으로 저장. 이후 카드를 한 장 쓸 때마다(`game:playCard`/`game:defend`/`game:playFieldCard` 전부) `drawReplacementCard()`가 덱에서 한 장을 뽑아 그 자리에 채워줘서 **손패가 항상 5장으로 유지**됨. 덱이 바닥나면 카드 풀 전체를 다시 셔플해서 채움(게임이 카드 부족으로 멈추지 않도록)
   - **필드 카드 효과 가시성**: `game:fieldCardPlayed`에 효과 상세 필드 추가 — 지형이면 `element`/`synergyBonus`/`boostedElement`(어떤 속성이 강화되는지, `elementTable.js`의 `GENERATE_MAP`으로 계산), 효과면 `tickDamage`/`durationTurns`. 필드 상태 표시줄도 "🗺️ 용암지대 (화속성 → 토속성 카드 +5) · ⚡ 전류 (매턴 5 피해, 2턴 남음)"처럼 무슨 효과인지 바로 보이게 함
   - **보너스 강조색**: 상극 보너스(`counterBonus`)/지형 보너스(`attackTerrainBonus`, `defenseTerrainBonus`)가 붙으면 전투 로그·방어 요청 팝업·필드 카드 사용 로그에서 `<span class="bonus-text">` (연노랑 `#fdcb6e`, 굵게)로 따로 강조 표시됨
   - **보안 주의사항**: 이 작업부터 combatLog/defenseInfo 렌더링이 `textContent`에서 `innerHTML`로 바뀐 부분이 있음(색깔 있는 `<span>`을 넣어야 해서). 닉네임은 사용자가 직접 입력하는 값이라 그대로 `innerHTML`에 넣으면 XSS 위험이 있어서, `main.js`에 `escapeHtml()` 헬퍼를 추가해 닉네임/카드이름을 항상 이스케이프한 뒤 삽입하도록 처리함. **앞으로 로그에 새 텍스트를 innerHTML로 추가할 때는 반드시 `escapeHtml()`을 거쳐야 함** (카드 이름처럼 서버 고정 데이터도 습관적으로 이스케이프하는 걸 권장)
   - 인메모리 테스트 스크립트로 10개 시나리오(공격/방어/지형/효과 카드 각각 사용 후 손패 5장 유지, 덱 소진 시 재보충, fieldCardPlayed 페이로드에 효과 정보 포함) 전부 통과 확인 후 삭제. 서버 재기동 완료 — 개발자가 브라우저에서 재테스트 예정

### Socket.io 이벤트 목록 (지금까지 구현된 것)

**클라이언트 → 서버**
- `room:create` `{ nickname }`
- `room:join` `{ roomId, nickname }`
- `room:leave` (payload 없음)
- `game:start` `{ roomId }`
- `game:endTurn` `{ roomId }`
- `game:playCard` `{ roomId, cardInstanceId, targetSocketId }` — 공격 카드 사용
- `game:confirmDefense` `{ roomId, cardInstanceIds: [] }` — 방어 카드 확정 사용 (6단계에서 `game:defend`/`game:skipDefense`를 완전히 대체함, 아래 참고). 빈 배열이면 방어 포기와 동일
- `game:playFieldCard` `{ roomId, cardInstanceId }` — 필드 카드(지형/효과) 사용, 대상 지정 없음

**서버 → 클라이언트**
- `room:created` `{ roomId }`
- `room:updated` `{ roomId, hostSocketId, status, players[](hp/maxHp/isAlive/handCount/attackCost/maxAttackCost/defenseCost/maxDefenseCost 포함), currentTurnSocketId, pendingDefense, terrain, effects[] }`
- `game:turnChanged` `{ currentTurnSocketId }`
- `game:handUpdated` `{ hand[] }` — 본인에게만 개별 전송 (instanceId + 카드 상세 정보: cost/element/attackPower/defensePower/synergyBonus/tickDamage/durationTurns 등, 카드 타입에 따라 해당 필드만 의미 있음)
- `game:attackAnnounced` `{ attackerSocketId, attackerNickname, defenderSocketId, defenderNickname, cardId, cardName, cost, element, attackPower, attackTerrainBonus }` — **(6단계 신규)** 공격 카드를 냈을 때 **방 전체**에 방송(공격자/방어자뿐 아니라 구경하는 사람도 포함). 화면 중앙에 공격 카드를 띄우는 연출용
- `game:defenseRequest` `{ attackerNickname, cardName, element, attackPower, attackTerrainBonus }` — 방어 대상에게만 개별 전송. 안내 문구용(실제 방어 카드 선택은 손패에서 직접 함)
- `game:defenseCardApplied` `{ defenderSocketId, cardId, cardName, cost, element, defensePower, counterBonus, terrainBonus, effectiveDefensePower }` — **(6단계 신규)** 방어자가 `game:confirmDefense`로 확정한 방어 카드 한 장마다 방 전체에 방송(여러 장이면 여러 번). 공격 카드 위에 겹쳐 쌓이는 연출용
- `game:combatResult` `{ attackerSocketId, defenderSocketId, cardId, cardName, defended, attackPower, attackTerrainBonus, appliedDefenses: [{ cardId, cardName, element, defensePower, counterBonus, terrainBonus, effectiveDefensePower }], defensePowerUsed, damageDealt, defenderHp, defenderIsAlive }` — 방 전체 방송. `appliedDefenses`는 이번 방어에 사용된 카드 전부(0장이면 빈 배열 = 무방비로 전부 피해)
- `game:fieldCardPlayed` `{ playerSocketId, playerNickname, cardId, cardName, fieldType, element, synergyBonus, boostedElement, tickDamage, durationTurns }` — 필드 카드 사용 시 방 전체 방송. `synergyBonus`/`boostedElement`는 지형일 때만, `tickDamage`/`durationTurns`는 효과일 때만 값이 있음(나머지는 `undefined`)
- `game:effectsTicked` `{ effects: [{ cardId, cardName, tickDamage, affectedSocketIds }] }` — 턴 전환 시 효과가 발동했을 때만 방송
- `game:over` `{ winnerSocketId, winnerNickname }`
- `error` `{ code, message }` — 코스트 관련: `INSUFFICIENT_COST`. 방어 관련: `NOT_DEFENDER`, `CARD_NOT_FOUND`, `CARD_NOT_DEFENSE` (여러 장을 한 번에 검증하므로 하나라도 문제 있으면 전체가 거부되고 아무 것도 반영되지 않음). 필드 관련: `CARD_NOT_FIELD` (`CARD_COST_MISMATCH`는 전투 시스템 개편으로 삭제됨)

### 아직 구현되지 않은 것 (다음 단계부터)
- 잡화 카드 (카드 교환, 필드 제거/강탈 등) (미착수)
- "필드 무시" 카드 (개별 회피) — 아직 미구현, 필드 보너스는 지금 전원에게 무조건 적용됨
- 카드 드로우: 지금은 게임 시작 시 5장 분배 후 소진되면 끝. 매 턴 드로우 여부/장수는 미정 (다음 설계 논의 필요)
- 방어 요청 제한시간(타임아웃) — 지금은 시간제한 없이 계속 대기
- "방어 성공 시 코스트 적립" 등 코스트 확장 메커니즘 — 검토 후 보류된 상태 (위 "코스트 시스템" 섹션 참고)
- 공격력/방어력을 동시에 가지는 특수 카드 — 데이터 구조상 이미 열어뒀지만(양쪽 필드 다 있음) 실제 카드는 아직 없음

---

## 4. 다음 단계 (지금부터 이어서 할 일)

설계했던 개발 순서 기준으로 **6단계**부터 진행하면 됩니다.

> 1. ~~방 생성/입장까지만 되는 뼈대~~ (완료)
> 2. ~~카드 없이 턴만 넘어가는 구조~~ (완료)
> 3. ~~카드 5~6장 정도로 최소 세트 만들어서 공격→방어턴→HP 감소 흐름 구현~~ (완료, 2026-08-25)
> 4. ~~코스트 시스템 + 속성 상성 판정 추가~~ (완료, 2026-08-25)
> 5. ~~필드(지형/효과) 시스템 추가~~ (완료, 2026-08-25)
> 6. ~~프론트엔드 UI 다듬기 (레이아웃/반응형/카드 비주얼/공격·방어 연출 전면 개편)~~ (거의 완료, 2026-08-27 — 자세한 내용은 아래 "6단계 진행 상황" 참고, 남은 다듬기 항목은 우선순위 상의 필요)
> 7. **다음 우선순위 상의** ← 여기부터 (남은 UI 다듬기 항목, 또는 "아직 정해지지 않은 것"/"미착수 항목" 중 하나)

### 5단계 결과 요약 (완료됨, 참고용)
- `Room.js`에 지형(`terrain`, 1슬롯 교체형) + 효과(`effects`, 다슬롯 누적형) 상태 추가
- **설계 결정**: 지형 카드는 코스트 무료, 효과 카드는 **공격 코스트 풀**에서 소모 (개발자와 상의해서 결정)
- 상생 보너스는 상극 보너스와 동일하게 **지금은 +5 고정값**, 카드 데이터에 `synergyBonus` 필드로 들고 있어서 나중에 카드별로 다르게 조정 가능하도록 구조를 열어둠 (개발자 요청사항)
- `game:playFieldCard` 신규 이벤트로 지형/효과 카드 사용 (대상 지정 없음), 턴 전환마다 `Room.tickEffects()`가 효과를 발동시켜 전원에게 피해를 주고 소멸 처리
- 자세한 내용은 위 "3. 기술 아키텍처 > 5번 항목" 참고. 17개 시나리오 테스트 전부 통과, 서버 재기동 완료 — 개발자가 브라우저에서 재테스트 예정

### 6단계 진행 상황 (문서 원래 계획: "프론트엔드 UI 다듬기") — 2026-08-27 기준 대부분 완료
개발자 피드백으로 여러 라운드에 걸쳐 진행됨. 초반 라운드(위 "3. 기술 아키텍처 > 6번 항목" 참고):
- ~~카드 자동 보충 (손패 항상 5장 유지)~~ (완료)
- ~~필드 카드(지형/효과) 사용 시 효과 내용 표시~~ (완료)
- ~~상극/지형 보너스를 강조색으로 표시~~ (완료)

이후 개발자가 그림판 스케치와 실물 카드 사진을 참고자료로 준 뒤 여러 라운드로 진행한 레이아웃/연출 개편:

**레이아웃 재구성 (스케치 기반)**
- `#game.game-board`를 `#app`(로비/대기실용 좁은 고정폭 컨테이너) 바깥의 형제 요소로 분리해서, 게임 화면은 `#app`의 폭 제한을 안 받음
- 4분할 배치: 좌상단(내 HP/코스트 + 지형/효과 슬롯) / 우상단(상대방 HP/코스트, 여러 명이면 세로로 쌓임) / 우하단(턴 표시 + 전투 로그 + 턴 종료 버튼) / 하단 전체(손패 부채꼴)
- 손패는 `--fan-offset`(중앙 0, 좌우로 갈수록 음/양수)·`--fan-lift`(중앙이 가장 위로 들림, CSS 변수 조합으로 부채꼴 회전+높이 표현)를 JS에서 정수로만 세팅하고, 실제 px/각도 환산은 style.css의 반응형 변수(`--card-x-step` 등)가 담당 — **중앙 카드가 항상 맨 위로 오도록** z-index를 인덱스 기준으로 부여
- 마우스오버 시 카드가 위로 더 튀어나오며 확대되는 hover 연출 추가

**반응형 스케일링**
- 처음엔 요소별로 각자 `clamp()`를 따로 줬더니 화면 크기별로 커지는 비율이 어긋나는 문제 발생 → **`.game-board`에 단 하나의 `font-size: clamp(...)`만 두고, 모든 하위 요소 크기를 `em` 단위로 통일**하는 방식으로 교체 (버튼/입력창은 기본적으로 `font-size`를 상속 안 받아서 `button, input { font: inherit; }` 별도 필요)
- 화면이 커지면 보드 자체의 `max-width`(레이아웃이 퍼지는 정도)와 `font-size`(요소 크기 배율)는 서로 독립적인 CSS 값이라, `max-width`만 넓혀도(1600px → 2400px) 카드 등 요소 비율은 전혀 깨지지 않음을 확인하고 반영 — 개발자가 "비율이 깨지면 불가능하다고 해달라"고 했던 요청에 대해 **가능하다고 판단하고 그대로 구현함**

**카드 비주얼 (실물 카드 사진 참고)**
- `buildCardFaceHtml(card)` 공용 함수로 카드 앞면 마크업(코스트 배지 / 이름 / 속성 아이콘 영역 / 하단 스탯) 통일 — 손패 카드, 화면 중앙 연출 카드, 필드 카드 미리보기가 전부 동일한 마크업을 공유
- 카드 타입별(공격/방어/지형/효과) 아이콘 영역에 파스텔톤 배경색 적용

**공격/방어 연출 (여러 라운드에 걸쳐 개편됨, 현재 최종 형태)**
- 공격 카드를 냈을 때 `game:attackAnnounced`를 **방 전체에 방송**해서, 공격자·방어자뿐 아니라 게임에 참여한 전원의 화면 중앙에 손패와 동일한 카드 모양으로 표시됨. **방어가 끝날 때까지(`game:combatResult`) 사라지지 않음**
- 방어자는 모달 팝업 대신 **본인 손패에서 직접 방어 카드를 클릭**해서 고름(여러 장 선택 가능). 화면 우하단 "사용" 버튼을 눌러야 서버에 실제로 반영되고, 좌하단 "취소" 버튼으로 서버에 아무 것도 안 보내고 지금까지의 선택만 로컬에서 되돌릴 수 있음
  - (배경: 처음엔 카드를 클릭하는 즉시 서버에 반영하는 방식이었는데, 방어 코스트가 다 떨어지면 남은 카드가 전부 비활성화돼서 진행을 못 하고 멈추는 버그가 있었음 → "로컬 선택 + 사용/취소로 일괄 확정" 방식으로 교체해서 해결)
  - 서버(`game:confirmDefense`)는 선택된 카드 전체를 **원자적으로 검증**(카드 존재 여부/타입/총 코스트)해서 하나라도 문제 있으면 전부 거부하고 아무 것도 반영하지 않음 (부분 적용 없음)
  - 방어 카드를 낼 때마다 공격 카드 위에 **겹쳐서 쌓이는 연출**(`game:defenseCardApplied` 방송, "막아내는 느낌")이 나오고, 결과가 확정되면(`game:combatResult`) 쌓여있던 카드들이 **전부 동시에 사라짐**
  - **(가장 최근 반영, 2026-08-27)** 방어 카드를 손패에서 클릭해 "선택"하는 그 순간 서버 응답을 기다리지 않고 곧바로 겹침 연출이 재생됨(내 화면에서만 미리 보여주는 로컬 프리뷰, 카드 기본 방어력으로 표시 — 상극/지형 보너스가 반영된 정확한 수치는 결과 확정 후 전투 로그에서 확인). 선택 해제하거나 "취소"를 누르면 미리 쌓았던 카드가 다시 빠짐. 방어자 본인 화면에서는 이후 서버가 보내는 `game:defenseCardApplied`를 중복 추가하지 않도록 무시하고(`defenderSocketId === socket.id`로 판별), 공격자/구경하는 다른 플레이어 화면에는 그대로 적용됨
  - 화면 중앙에 뜨는 공격 카드는 손패 카드보다 **10% 더 크게** 표시됨
  - `#combatOverlay`(연출용 풀스크린 레이어)는 `pointer-events: none`으로 처리해서, 연출이 떠 있는 동안에도 그 아래의 손패 클릭(방어 카드 선택)이 막히지 않음 (처음엔 이 레이어가 클릭을 가로채서 방어 카드를 못 고르는 버그가 있었음)
- 지형/효과 카드는 클릭하면 공격 카드처럼 화면 중앙에 먼저 떠서 미리보기 상태가 되고, 한 번 더 클릭하면 실제로 사용됨(바깥을 클릭하면 취소)
- 지형 카드를 사용하면 배경 색/그라디언트가 지형 속성에 맞게 바뀜(`ELEMENT_BACKGROUNDS`/`applyTerrainBackground`) — 이미지 에셋이 없어서 색/그라디언트 테마로 대체하기로 개발자와 합의함

남은 다듬기 범위 제안 (개발자가 다음에 우선순위 정하면 됨):
- 로딩/연결 끊김 상태 처리 (Socket.io 재연결 시 화면이 어떻게 되는지 지금은 `#connectionBanner`로 안내만 하고 깊게 신경 안 씀)
- 데미지 발생 시 HP 감소를 숫자가 튀거나 바가 줄어드는 애니메이션으로 보여주는 등, 결과 확정 이후의 추가 시각 피드백
- 모바일/좁은 화면에서의 실사용성 재확인 (반응형 스케일링 자체는 되어 있으나 실기기 테스트는 아직 안 함)

### 참고: 아직 미착수 상태로 남아있는 것 (6단계와 별개로, 우선순위는 개발자와 상의)
- 잡화 카드, "필드 무시" 카드 — 문서 2장 "아직 정해지지 않은 것" 항목
- 방어 요청 타임아웃, 재접속(reconnect) 처리 등 견고성(robustness) 관련 작업

---

## 5. 진행 방식 요청사항

- 이 프로젝트를 진행하는 사람은 학생이고, Java/Spring Boot 배경이 있지만 Node.js/JS는 처음 다뤄보는 수준입니다. 코드에 주석을 충분히 달아주시고, 새로운 개념이 나오면 간단히 설명을 곁들여주세요.
- 큰 변경 전에는 무엇을 할 계획인지 먼저 설명하고 진행해주세요 (설계 논의 후 구현하는 흐름을 유지해왔습니다).
- 각 기능 구현 후 실제로 서버가 정상 실행되는지, 문법 오류는 없는지 확인해주세요.
