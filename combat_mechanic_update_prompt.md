# 전투(공격/방어) 시스템 변경 — Claude Code 작업 지시

기존 `HANDOFF.md`에 정리되어 있던 **코스트 매칭 방어 시스템은 완전히 폐기**하고, 아래 새 시스템으로 교체해주세요. 아직 카드/전투 로직 자체는 코드로 구현되기 전 단계이므로, 기존 코드에 영향은 적을 것으로 예상되지만 `HANDOFF.md` 문서는 반드시 이 내용으로 갱신되어야 합니다.

---

## 1. 폐기되는 규칙

`HANDOFF.md`의 "코스트 시스템" 섹션 중 아래 내용은 전부 삭제/대체 대상입니다.

- ~~공격 카드와 동일한 코스트의 방어 카드로만 방어 가능~~
- ~~상극 관계면 코스트를 추가 지불해서 낮은 코스트 카드로도 방어 가능~~
- ~~상극 아니면 정확히 동일 코스트로만 방어 가능~~

## 2. 새로운 공격/방어 시스템

### 코스트는 이제 "행동 자원 풀"입니다 (매칭 판정이 아님)

- **공격 코스트**: 자기 턴에 지급되는 코스트 풀 (기본값 예: 3, 정확한 수치는 추후 확정). 이 풀 안에서 **여러 번, 여러 대상에게** 공격 카드를 낼 수 있음
  - 예: 1코스트 카드 3번 사용 / 3코스트 카드 1번 사용 / 1+2코스트 조합 등 자유
  - 카드나 필드 효과로 이 풀 자체를 증감시킬 수 있음 (예: "공격 코스트 +1" 효과)
- **방어 코스트**: 방어자도 별도의 코스트 풀을 보유. 상대가 자기 턴 동안 여러 번 공격해와도, 방어자는 매번 자신의 방어 코스트 풀에서 소모하며 대응
  - 마찬가지로 카드/필드 효과로 증감 가능
- 공격 코스트 풀과 방어 코스트 풀은 **서로 다른 자원**이며 독립적으로 관리됨

### 카드 스탯: 공격력 / 방어력

- **기본 원칙**: 공격 카드는 **공격력**만 가지고, 방어 카드는 **방어력**만 가짐 (역할이 분리됨)
- 예외적으로 공격력과 방어력을 동시에 가지는 특수 카드도 나중에 추가될 수 있음 (지금 단계에서 미리 설계할 필요는 없고, 카드 데이터 구조상 두 필드를 다 가질 수 있게만 열어두면 충분함)

### 데미지 계산 방식

코스트는 "이 카드를 낼 수 있는가(자원이 충분한가)"만 결정하고, **실제 피해량은 공격력과 방어력의 수치 비교**로 계산합니다.

- 공격력 10 vs 방어력 15 → 방어 성공, 데미지 0
- 공격력 10 vs 방어력 5 → 방어 실패, 데미지 5 (공격력 - 방어력)
- 방어자가 아예 방어 카드를 못 내거나(방어 코스트 부족) 포기하면 → 공격력 그대로 전부 피해

### 속성(오행) 보너스 방식 변경

- 기존의 "상극이면 코스트 추가 지불해서 할인" 방식은 폐기
- 대신 **상극 관계면 공격력 또는 방어력에 수치 보너스**를 부여하는 방식으로 변경
  - 예: 방어 속성이 공격 속성을 상극하는 관계라면 방어력에 보너스 (구체적 보너스 수치는 카드 밸런싱 단계에서 확정 예정, 지금은 구조만 잡아두면 됨)
- 오행 상극/상생 순환 구조 자체는 변경 없음 (상극: 목→토→수→화→금→목 / 상생: 목→화→토→금→수→목)
- 상생의 "필드 지형 연동 강화" 역할도 변경 없음

### 턴 진행 흐름 (기존 규칙과 결합)

턴 순서 자체(순번대로 진행)는 기존 그대로 유지됩니다. 달라지는 것은 "한 턴 안에서 벌어지는 일"입니다.

- 자기 턴에 공격 코스트가 남아있는 한, **여러 번 공격 카드를 낼 수 있고 매번 다른 대상을 지정할 수도 있음** (타겟은 매 공격마다 자유롭게 선택, 턴 자체는 계속 그 사람의 턴)
- 공격 하나가 나갈 때마다: 공격 → 대상이 방어 카드 지정(또는 포기) → 데미지 계산 → 결과 반영, 이 사이클이 공격자의 코스트가 소진될 때까지 반복
- 방어자는 자신의 방어 코스트가 허용하는 한 계속 막을 수 있지만, 여러 번 공격받으면 자신의 방어 코스트도 그만큼 소모되므로 한 턴 안에서 집중 공격을 받으면 방어력이 바닥날 수 있음

흐름 예시:

```
A의 턴, 공격 코스트 3
1. A → B 공격 (1코스트 사용) → B가 방어 카드로 대응 (B의 방어 코스트 사용) → 데미지 계산
2. A → C 공격 (1코스트 사용) → C가 방어 카드로 대응 → 데미지 계산
3. A → B 공격 (남은 1코스트 사용) → B가 남은 방어 코스트로 다시 방어 시도
4. A의 공격 코스트 소진 → 턴 종료, 다음 사람 턴으로
```

---

## 3. Claude Code에 요청하는 작업

1. `HANDOFF.md`에서 "코스트 시스템"과 "속성 시스템(오행)" 관련 섹션을 이 문서 내용에 맞게 갱신해주세요. 특히 "방어 판정" 관련 서술은 완전히 교체 대상입니다.
2. 만약 이미 `combatResolver.js`, `elementTable.js` 등 전투 판정 관련 코드를 작성한 상태라면, 새 방식(코스트=자원 풀, 데미지=공격력/방어력 비교)에 맞게 로직을 다시 설계해주세요. 아직 작성 전이라면 이 문서 내용을 기준으로 처음부터 설계하면 됩니다.
3. `Room.js`의 플레이어 상태 구조에 `attackCost`(현재 턴 공격 코스트), `defenseCost`(현재 턴 방어 코스트) 같은 필드가 필요한지 검토하고, 필요하면 추가해주세요.
4. 카드 데이터 구조(`cards.json` 또는 이에 준하는 것)에 `attackPower`, `defensePower` 필드를 반영해주세요 (공격 카드는 attackPower만, 방어 카드는 defensePower만 값이 있고 나머지는 0 또는 null).
5. 변경 후 서버가 정상적으로 실행되는지(문법 오류 등) 확인해주세요.

작업 시작 전에 위 설계에 대해 궁금한 점이나 애매한 부분이 있으면 먼저 질문해주세요. 특히 아래는 아직 미확정이니 임의로 정하지 말고 개발자와 상의해서 정해주세요:
- 기본 공격/방어 코스트 수치 (턴당 지급량)
- 오행 상성 보너스의 정확한 수치
- 손패 상한, 시작 HP 등 다른 밸런스 수치
